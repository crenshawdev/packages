# Cloudflare Worker: Ghost -> Cloudflare cache-purge intermediary.
# Deploy from this directory: `npx wrangler deploy`
# Set secrets first (see README.md): PURGE_SECRET, CF_API_TOKEN.
name = "cache-purge"
main = "worker.js"
compatibility_date = "2026-01-01"

[vars]
# The jcrenshaw.dev zone id (Cloudflare dashboard -> the zone -> Overview,
# right sidebar). Not a secret, safe to commit.
CF_ZONE_ID = "2d789a860ef460686f494e43cd6b5164"
